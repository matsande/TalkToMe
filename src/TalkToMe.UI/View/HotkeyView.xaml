<UserControl
  x:Class="TalkToMe.UI.View.HotkeyView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:TalkToMe.UI.View"
  xmlns:control="clr-namespace:TalkToMe.UI.Control"
  mc:Ignorable="d"
  d:DesignHeight="300"
  d:DesignWidth="300">
  <DockPanel Background="LightGray" LastChildFill="False">
    <StackPanel DockPanel.Dock="Top">
      <StackPanel
        Orientation="Horizontal">
        <TextBlock>Speak</TextBlock>
        <control:HotKeySelection KeyMonitor="{Binding KeyMonitor, Mode=OneWay}" MinWidth="200" />
      </StackPanel>
      <StackPanel
        Orientation="Horizontal">
        <TextBlock>Auto Mode</TextBlock>
        <control:HotKeySelection KeyMonitor="{Binding KeyMonitor, Mode=OneWay}" MinWidth="200" />
      </StackPanel>
    </StackPanel>
    <DockPanel
      DockPanel.Dock="Bottom">
      <Button
        Content="Apply"
        DockPanel.Dock="Right" />
    </DockPanel>
  </DockPanel>
</UserControl>